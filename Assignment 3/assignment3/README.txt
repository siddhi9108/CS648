# Assignment 3 — Starter (from scratch)

Open `index.html` in your browser to view the site. Edit `styles/styles.css` to adjust styles.
Pages: Home, About, Solutions, Pricing, Contact. All pages share the external stylesheet.

The Home page also contains a small embedded `<style>` block that uses a CSS3 selector to bold
the solution titles without relying on classes/IDs (as required in Part 2).
